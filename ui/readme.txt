put these ui folder in your Xampp/htdocs folder.
your can enter manual essay -->run the file essayincsv in your browser (its a php file so use xampp) these newly added esay is stored in ui/essay.csv file (donot modify the geader of essay.csv file) 

when running AEG_UNSEEN it will ask  you to choose the .csv file then choose the same essay.csv file and run the colab nb.it will generate the output file with graded essayscore .you can view it on colab nb(ouput.csv ka head)OR open theoutput.csv file it generates OR
for UI purpose check the UI/viewscore
remember the ui file and the other files inside it needs to be placed and accessed through htdocs folder only